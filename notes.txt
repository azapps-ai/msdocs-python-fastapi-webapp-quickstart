az group create --name web-app-fastapi-an --location eastus
az acr create --resource-group web-app-fastapi-an --name webappacran123 --sku Basic --admin-enabled true

ACR_PASSWORD=$(az acr credential show \
    --resource-group web-app-fastapi-an \
    --name webappacran123 \
    --query "passwords[?name == 'password'].value" \
    --output tsv)

az acr build --resource-group web-app-fastapi-an --registry webappacran123 --image webappsimple:latest .